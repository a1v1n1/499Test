# 499Test
Running the sorter will prompt the user to write either "int" or "string" to choose which kind to sort.
It will then ask for the file path of the text file to sort (eg. C:\Documents\stringTest.txt). The sorted list is printed out.
